import { NextResponse } from "next/server";

export async function GET() {
  return NextResponse.json({
    message: "Google OAuth integration placeholder. Replace with real OAuth flow in production.",
  });
}
